import Canvas from './canvas'

const BASIC_WIDTH = 60
const BASIC_HEIGHT = 110
const standard = require('./standard.json')

var GameBuilder = {
  onUpdate: function(ctx, G){
    // this.gameBoard


  }
}

export default GameBuilder;
